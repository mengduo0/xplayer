---
description: XPlayer 强制自动化闭环：改码→编译→自测→失败则改→再测，直至通过
alwaysApply: true
---

# XPlayer 自动化开发工作流（强制）

**每一次**代码/配置改动后，必须跑完整闭环，**禁止**未自测就向用户汇报完成。未明确要求时 **禁止** `git commit` / `git push`。

## 闭环（循环直至通过）

```
理解需求 → 改码 → dev-verify → 失败？→ 修码 → dev-verify → … → DEV-VERIFY PASSED → 汇报
```

Agent **必须亲自执行** Shell，不要只写「请运行」。

## 1. 改码前

- 明确验收标准（API、播放、MySQL 数据等）
- 先读相关文件：`backend/src/main/java/com/xplayer/`、`frontend/src/`、`scripts/`

## 2. 改码

- 最小 diff，匹配现有风格
- 默认 **MySQL**（Docker）；勿设 `SPRING_PROFILES_ACTIVE=h2`，除非用户明确要求离线模式

## 3. 自测（每次改码后必跑）

```powershell
cd H:\work\xplayer
.\scripts\dev-verify.ps1
```

等价于：后端 Maven 打包 → 前端 `npm run build` → `run-automation.ps1`（MySQL）→ `run-browser-test.ps1`（Playwright 无头浏览器）。

**成功标志**：`AUTOMATION PASSED`、`BROWSER-TEST PASSED`、`DEV-VERIFY PASSED`。

单独跑浏览器测试：`.\scripts\run-browser-test.ps1`（本机 Edge，npm/playwright 走 npmmirror；无需下 180MB Chromium）。

## 4. 失败则循环

- 根据终端报错改代码
- **从步骤 3 重来**，直到 `dev-verify.ps1` 通过
- 不得跳过编译或冒烟

## 5. 环境

| 项 | 值 |
|----|-----|
| 根目录 | `H:\work\xplayer` |
| 视频 | `G:/mv`（`xplayer.media-root`） |
| MySQL | `.\scripts\ensure-mysql.ps1`（需 Docker Desktop） |
| 后端 | `http://127.0.0.1:8080` |
| 前端 | `http://127.0.0.1:5173` |

首次或依赖变更：`.\scripts\setup.ps1`

## 6. 手动联调（可选，在 dev-verify 通过后）

```powershell
.\scripts\run-all.ps1
```

## 7. 数据

- 灌库：`.\scripts\seed-from-mv.ps1`
- 结构：`sql/init.sql`

## 8. 汇报与提交

- 汇报：改了什么、跑了什么命令、自测结果
- 全部通过后再问用户是否提交；禁止自动 commit/push
